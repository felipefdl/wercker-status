box: felipefdl/atom
build:
    steps:
        - script:
            name: install dependencies
            code: npm install
        - script:
            name: build
            code: apm test
        - script:
            name: environment info
            code: apm --version
